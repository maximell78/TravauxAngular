import { QtePositivePipe } from './qte-positive.pipe';

describe('QtePositivePipe', () => {
  it('create an instance', () => {
    const pipe = new QtePositivePipe();
    expect(pipe).toBeTruthy();
  });
});
